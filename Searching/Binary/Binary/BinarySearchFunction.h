#pragma once

template <typename T>
int Calculate(T* arr, T vallue, int Size);//calculate similar elements

template <typename T>
void Sort(T* arr, int size);

template <typename T>
T* SearchBuble(T* arr, T vallue, int left, int right, int Size);

